{
  "name": "workout-tracker-mern",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm install --prefix backend-rest && npm install --prefix frontend-react && npm run build --prefix frontend-react",
    "start": "npm start --prefix backend-rest"
  }
}